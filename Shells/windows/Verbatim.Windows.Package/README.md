# Verbatim Windows Package

This folder is the tracked companion package root for the Windows shell.

The WinUI app remains a single-project packaged application at `Shells/windows/Verbatim.Windows/`.
Generated Windows package artifacts are staged under `AppPackages/` by the repo-level PowerShell workflow.
