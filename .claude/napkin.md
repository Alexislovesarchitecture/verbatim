# Napkin

## Corrections
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|----------------|-------------------|
| 2026-03-03 | self | Started repo exploration before establishing persistent napkin file because it did not exist yet | Create `.claude/napkin.md` immediately when missing, then continue technical exploration |
| 2026-03-03 | self | Assumed `gh repo fork --remote --remote-name fork` would add a `fork` git remote in this repo, then push failed because remote was absent | Verify remotes with `git remote -v` immediately after forking and add the intended remote explicitly before pushing |
| 2026-03-03 | self | Tried using `remote.origin.pushurl` to redirect pushes while keeping `origin.url` on upstream; `--force-with-lease origin` compared lease against upstream tracking ref and rejected as stale | For fork workflows that use `--force-with-lease`, set `origin` to the writable fork and keep canonical repo as separate `upstream` remote |
| 2026-03-03 | self | Used main-actor-isolated service initializers as default arguments in a main-actor view model init; Swift treated call-site as nonisolated and failed compile | Keep service types nonisolated unless actor isolation is required, or construct defaults inside initializer body |
| 2026-03-03 | self | Mixed `HierarchicalShapeStyle` (`.secondary`) and `Color` (`.orange`) in one ternary for `.foregroundStyle` | Use explicit `Color.secondary` / `Color.orange` when mixing style branches |
| 2026-03-04 | self | Reintroduced mixed `.foregroundStyle` ternary styles in `ContentView.swift`, breaking `swift build` | When writing ternaries for style/color, always use explicit `Color.*` in both branches and run a build immediately after UI edits |
| 2026-03-03 | user | Friendly aliases (Mini/Turbo) were interpreted as desired labels, but user wants official model names only | Display OpenAI models exactly by API-provided model IDs; avoid custom marketing labels |
| 2026-03-03 | self | Parsed `known_speaker_references` using comma splitting, which corrupts data URLs because they contain commas | Parse speaker references line-by-line only; keep comma splitting limited to speaker-name convenience input |
| 2026-03-03 | self | `withCheckedThrowingContinuation` in a complex callback failed type inference after refactor | Use explicit continuation type (`CheckedContinuation<ExpectedType, Error>`) when inference becomes ambiguous |
| 2026-03-03 | self | `@Published` `didSet` for `transcribeUseDiarization` called `applyTranscriptionModelSelectionDefaults()`, which reassigned the same property and caused infinite observer recursion + EXC_BAD_ACCESS on launch | In property observers that trigger normalization, gate recursive calls with `if oldValue != newValue` and make normalization idempotent |
| 2026-03-03 | self | Logic formatting failed with API error `unsupported value 'none'` for `reasoning.effort` | Use only supported effort values (`minimal|low|medium|high`), send reasoning only for GPT-5 models, and add one fallback retry with `minimal` or no reasoning on 400 reasoning validation errors |
| 2026-03-04 | self | Added a public/static helper that accepted a private enum parameter, causing module emit failure | Keep migration-only enums/helpers at matching access level (`private`/`fileprivate`) and run a build after refactoring Codable migration code |
| 2026-03-03 | self | Pushes from diverged local history failed with `remote unpack failed` / missing object; normal and force pushes both rejected | When history is damaged, create a clean commit from `main^{tree}` with parent `origin/main` via `git commit-tree` and push that commit to `main` |
| 2026-03-03 | self | Partial Swift file write from shell heredoc was truncated by quoting, leaving an incomplete source file | After large heredoc writes, immediately `cat`/verify file content before continuing and overwrite atomically if output is truncated |
| 2026-03-04 | self | Announced that napkin was read, which violates the napkin skill instruction to apply it silently | Read and apply napkin rules without stating that action in user-facing updates |
| 2026-03-04 | self | Introduced malformed Swift keypaths (`\\ .id`) while writing a new file quickly, which would fail compilation | After creating files via heredoc, immediately scan for escaped keypaths (`\\.`) and other syntax artifacts before proceeding |
| 2026-03-04 | self | A bulk regex removed an iOS availability line incorrectly and produced `@availablefinal`, breaking syntax | After bulk annotation rewrites, run a fast syntax scan (`rg \"@availablefinal|@available\\(\"`) and repair malformed joins before continuing |
| 2026-03-04 | self | Platform-annotation cleanup via regex caused wide declaration corruption (`@availableprotocol`, `@available@main`) | Prefer a single safe strategy: remove per-type availability annotations entirely when package platform already constrains target, then verify with `rg \"@available\"` and build |
| 2026-03-05 | self | Repo build failed because core services carried stray `@available(macOS 26.0, *)` annotations while `Package.swift` still targeted macOS 14 | Keep Tahoe-only checks centralized in a shared platform wrapper/helper and strip 26-only annotations from non-UI services before refactors |
| 2026-03-05 | self | Switched `Package.swift` to `.macOS(.v26)` and `swift-tools-version: 6.2`, which compiled the manifest but also upgraded SwiftPM language policy and exposed unrelated Swift 6 concurrency errors | For Tahoe-only targeting in this repo, keep `swift-tools-version: 5.9` and use `.macOS("26.0")` so deployment moves forward without pulling in a broader compiler-mode migration |
| 2026-03-05 | self | Assumed a small Tahoe-only UI cleanup would preserve both build entry points, but `swift build` exposed unrelated modified service files while the Xcode app target still built cleanly | In this repo, verify `swift build`/`swift test` and `xcodebuild` separately after UI-only changes; a green app target does not prove the package target is green |
| 2026-03-05 | self | `@MainActor` service types in default initializer arguments broke SwiftPM builds and tests even though constructing the same services inside the initializer body was valid | When a dependency is main-actor isolated, default the parameter to `nil` and instantiate it inside the `@MainActor` initializer body; mirror that isolation in tests instead of loosening the production types |
| 2026-03-05 | self | Reached for `python` when running the shared imagegen skill CLI, but this environment only exposes `python3`/`uv run ... python` | For skill CLIs in this repo, prefer `uv run --with ... python <script>` first; fall back to `python3` instead of assuming `python` exists |
| 2026-03-05 | self | The first `swift test` attempt failed with `input file ... was modified during the build` for the generated package runner even though source compilation was clean | If SwiftPM reports an object file changed during link on this machine, rerun the same build/test once before treating it as a code regression |
| 2026-03-06 | self | While refactoring `AudioRecorderService`, I moved tap IO off the main actor but initially forgot to recreate the local `AVAudioConverter` before wiring the stream state | After recorder or stream setup refactors, re-open the whole `startRecording()` flow top-to-bottom and verify resource creation order before rebuilding |
| 2026-03-07 | self | Live mic conversion to interleaved `pcmFormatInt16` failed inside `AVAudioConverter.convert(to:from:)` and stalled recording on start | Keep the live converter on mono `pcmFormatFloat32` and quantize to PCM16 manually after conversion instead of asking `AVAudioConverter` for direct `Int16` tap output |
| 2026-03-07 | self | Diarization evaluation failed when I reused full-length speaker reference clips; the API rejected known speaker references longer than the supported range | Keep known-speaker reference clips trimmed to roughly 1.2–10 seconds before exercising `gpt-4o-transcribe-diarize` with speaker hints |
| 2026-03-07 | self | While updating async test doubles, I assigned captured state and then left the final value as a bare expression, which Swift did not treat as an implicit return | In async test helpers, use an explicit `return` after state mutation instead of relying on last-expression shorthand |
| 2026-03-07 | self | Reworked the Style page around a new hero/tab layout but forgot to add the small `StyleCategoryShowcase` helper type before compiling | When introducing a new view helper model during a UI refactor, add the supporting type in the same patch as the first caller and run a build immediately |
| 2026-03-07 | self | In the first transcript resolver pass, I reused substring ranges from a lowercased copy against the original `String`, which is unsafe and easy to break in Swift | When doing case-insensitive cue parsing, convert ranges back by offset or stay in one string representation instead of reusing indices across different `String` values |
| 2026-03-07 | self | The first tail-replacement heuristic grabbed the earliest useful anchor (`send`) instead of the nearest local anchor (`to`), collapsing too much of the clause | For local rewrite heuristics, choose the last relevant anchor in the clause, not the first matching one |
| 2026-03-07 | self | A follow-up rewrite case (`actually`, `rewrite that`, then a full replacement clause) still used tail splicing and produced a hybrid sentence instead of replacing the whole clause | When a replacement clause has high overlap with the original, prefer whole-clause replacement over local tail splicing |
| 2026-03-07 | self | Added fallback glossary helpers in `TranscriptionViewModel` without re-scanning the lower half of the file and duplicated existing dictionary migration/reload methods | Before patching a large SwiftUI view model, search the whole file for existing helpers by name and extend them in place instead of adding parallel versions |
| 2026-03-07 | self | I first threaded diagnostics only through hotkey session context, which would have silently excluded manual recordings from the new diagnostics view | When adding session-scoped telemetry, decide up front whether manual and hotkey paths both need context, then make lock/freeze behavior explicit instead of using `nil` to mean both “manual” and “no diagnostics” |
| 2026-03-07 | self | In the first hotkey verification pass, I referenced the `HotkeyStartResult` from inside the registration closure before the result had been assigned | When a callback needs startup metadata from the same registration call, capture an optional placeholder, assign the result immediately after registration, and read through that placeholder inside the callback |
| 2026-03-07 | self | Bumped the diagnostics schema version but missed the corresponding schema-version assertion in `TranscriptRecordStoreTests`, causing a avoidable test failure on the first `swift test` pass | When adding a SQLite schema migration here, update both migration logic and the explicit `user_version` test in the same patch before running the suite |
| 2026-03-08 | self | Added `dependencies:` before `products:` in `Package.swift`, which SwiftPM rejected even though the contents were otherwise valid | When editing `Package.swift`, preserve the initializer argument order exactly and run a manifest parse via `swift test` immediately after the first package patch |
| 2026-03-08 | self | Started with a remote binary target URL for `whisper.cpp`, but SwiftPM artifact resolution against GitHub was flaky in this environment and blocked the build | For official XCFramework integrations here, prefer vendoring the minimal local slice when remote SwiftPM artifact downloads are unreliable on this machine |
| 2026-03-08 | self | Assumed adding defaulted `let` fields to a Swift struct would extend the synthesized memberwise initializer for downstream call sites, which broke diagnostics/request construction | When extending shared request or record structs here, add an explicit initializer in the same patch instead of relying on synthesized memberwise behavior |
| 2026-03-08 | self | Wrote a new XCTest numeric assertion against `Float?` with the `Double` accuracy overload, which broke the test target even though the production code compiled | In new audio/math tests here, unwrap optional sample values explicitly and compare as `Double` or `Float` with a matching overload before rerunning `swift test` |
| 2026-03-08 | self | Reintroduced the Core Audio converter capacity trap in the Whisper file-loader path by sizing the output buffer only to the resampled estimate, which can be smaller than the input frame length | For every `AVAudioConverter.convert(to:from:)` call here, size output capacity to `max(inputFrameLength, estimatedConvertedFrames)` and keep a test that exercises a downsampled input |
| 2026-03-08 | self | Made the managed WhisperKit helper response type `private` in one file, then reused it from another helper file and hit a module-scope compile failure | When two Swift files share a transport payload here, keep the type at internal scope or colocate it with the caller before rerunning the build |
| 2026-03-08 | self | Patched `WhisperKitLocalTranscriptionService.swift` while `swift test` was still compiling, which produced a noisy `input file was modified during the build` failure and stale diagnostics | If a long SwiftPM build is active and a fix is obvious, stop the build or let it fail first, then patch and rerun cleanly instead of editing the same source mid-build |
| 2026-03-08 | self | Assumed a green package build meant the SwiftUI executable would launch as a normal app, but LaunchServices classified the package binary as `BackgroundOnly`, leaving a live process with a hidden window | When a Swift package macOS app “builds but does not open,” inspect `lsappinfo list` and force `.regular` activation plus an initial `activate`/`makeKeyAndOrderFront` path before chasing view code |
| 2026-03-08 | self | Wrote a large `apply_patch` against `ManagedLocalTranscriptionService` using stale context after intermediate refactors, which wasted a cycle before I re-opened the exact file slice | Before applying a broad structural patch in this repo, re-read the exact current file region and patch against the live context instead of relying on a previously captured excerpt |
| 2026-03-09 | self | Added prewarm idempotence to `ManagedWhisperKitRuntime`, but the first test still re-prewarmed because the mocked `/health` response reset `activeModel` and `prewarmState` back to nil/idle | When lifecycle state is driven by health probes here, test doubles must return the same state transitions the real helper would expose or idempotence assertions will be false negatives |
| 2026-03-09 | self | Initially blamed the new WhisperKit helper/runtime when local transcription still failed, but the persisted sessions and OSLog showed every backend was dying earlier in `AudioRecorderService` with `AVAudioConverter` `OSStatus -50` during live 48 kHz -> 16 kHz resampling | When transcription fails across Apple Speech, WhisperKit, legacy Whisper, and remote OpenAI, inspect recorder diagnostics and `log show` before changing STT code; if the mic path is already native float mono, record native format and defer canonicalization to downstream consumers |
| 2026-03-09 | self | The Apple Dictation migration looked complete in UI/tests, but `ManagedLocalTranscriptionService` still short-circuited the Apple path before recording a route resolution, so diagnostics missed `apple_speech` / `in_process` metadata | When migrating backend routing here, verify the shared route tracker on the primary success path, not just the backend implementation and UI state |
| 2026-03-09 | self | The first dev-app bundle wrapper kept failing before any permission prompt because the packaged binary still needed `@executable_path/../Frameworks` for `whisper.framework`, and copied bundles/frameworks carried xattrs that broke ad-hoc signing | For SwiftPM macOS app wrappers here, patch the executable rpath for `Contents/Frameworks`, strip copied xattrs before signing, and verify launch with crash logs/TCC instead of assuming the plist keys alone are enough |
| 2026-03-09 | self | The first external-Whisper pivot still ran local WhisperKit runtime and install checks before honoring `.externalServer`, which blocked the very backend split the user wanted | When adding an external-server path here, branch before local runtime/model validation and test the bypass with an unsupported local runtime to prove the boundary is real |
| 2026-03-09 | self | I reused `Result<Transcript, String>` while wiring the external server fallback, forgetting that Swift `Result` failures must conform to `Error` | For lightweight transport helpers here, either return a custom `LocalizedError` wrapper or avoid `Result` entirely; do not use raw `String` as a `Result` failure type |
| 2026-03-09 | self | Started a large onboarding implementation in files that already had staged and unstaged changes for the same external-Whisper pivot | When the worktree is dirty in the same SwiftUI/view-model files, inspect both live content and staged/unstaged diffs before broad patches so new work intentionally replaces or preserves the in-flight behavior |
| 2026-03-09 | self | I kicked off `swift test` and `swift build` in parallel against the same SwiftPM `.build` directory and created a fake “input file modified during build” failure | Do not run concurrent SwiftPM build/test commands in the same repo unless they use isolated build directories; serialize them to avoid self-induced artifact races |
| 2026-03-09 | self | A broad `apply_patch` against `TranscriptionViewModel.stop()` failed because the live method had shifted under earlier setup changes in the dirty worktree | Re-read the exact method slice immediately before replacing a large control-flow block and patch against the current file instead of a stale excerpt |
| 2026-03-10 | self | Announced in commentary that I was reading/using the napkin skill, which this repo explicitly says to apply silently | Read and apply napkin guidance without mentioning that step in user-facing updates |
| 2026-03-10 | self | Trusted a green `xcodebuild` from the filesystem-synced app target even though its synced source folder was missing, so the build produced an empty app shell | Before trusting a green Xcode app build here, verify the target’s synced source/resource root actually contains the live app files |
| 2026-03-10 | self | Assumed `AVCaptureDevice.requestAccess(for: .audio)` had a direct async form in the native rewrite, which broke compilation | Bridge callback-only permission APIs with `withCheckedContinuation` unless the SDK actually exposes an async overload |
| 2026-03-10 | self | Passed onboarding actions as trailing closures and let Swift bind them to the `@ViewBuilder content` parameter instead of the `action` parameter | When a helper has both `content` and `action`, call it with explicit argument labels or an overload so SwiftUI doesn’t treat a side-effect closure as a `View` |
| 2026-03-10 | self | SwiftPM went green while the Xcode app target still failed because `ObservableObject`/`@Published` types were missing explicit `Combine` imports in app-target compilation | After a green `swift test`, still run `xcodebuild` here and add explicit `Combine` imports for any `ObservableObject`/`@Published` types used by the app target |
| 2026-03-10 | self | Bundled native runtimes and copied assets carried extended attributes into the built app, which made `codesign` fail late in the Xcode build | For copied binaries/assets in this repo, add a pre-sign `xattr -rc "${TARGET_BUILD_DIR}/${FULL_PRODUCT_NAME}"` build phase instead of assuming source-tree metadata is clean |
| 2026-03-10 | self | Even with a pre-sign xattr cleanup phase, the synced-root Xcode target still picked up late bundle metadata and failed only at the final CodeSign step during local `xcodebuild` verification | For local repo verification builds here, disable signing on the app target configs (`CODE_SIGNING_ALLOWED = NO`, `CODE_SIGNING_REQUIRED = NO`) unless you are explicitly validating notarization/signing |
| 2026-03-10 | self | After moving the active app root, a broad follow-up `apply_patch` against `README.md` failed because the live file still contained the old path block I had mentally normalized already | After structural renames here, re-read the exact live slices of docs/config files before broad replacement patches instead of patching from the intended end state |
| 2026-03-10 | self | Packaging/signing the copied `.app` inside this Desktop-synced repo kept reintroducing `com.apple.FinderInfo` and File Provider metadata, so ad-hoc signing failed despite `xattr -cr` | For local signed handoff from this repo, copy the built app to a neutral path like `/tmp/verbatim-release/` before stripping xattrs and signing |
| 2026-03-10 | self | The first native Apple Speech provider implementation scanned `AssetInventory.status` across every supported locale and used a different analyzer flow than the previously working helper, which matched the later `Speech.framework` crash pattern | In this repo, keep Apple Speech aligned with the proven helper path: use `SpeechTranscriber.installedLocales` for inventory and `SpeechAnalyzer.analyzeSequence` with `SpeechTranscriber`, not a per-locale asset sweep |
| 2026-03-10 | self | Tried `log show ...` directly in `zsh`, which invoked the shell's `log` builtin instead of macOS unified logging | Use `command log show ...` when querying the system log from `zsh` in this repo |

## User Preferences
- Keep successful behavior intact and layer visual enhancements instead of feature rewrites.
- Keep the Verbatim product name, brand assets, and overall aesthetic intact while replacing internals/features.
- Prioritize information architecture over pure polish: the left rail should be brand + Home / Dictionary / Style, with model and app controls behind a bottom settings pop-up.
- Keep OpenAI model names exactly as OpenAI provides them (no custom renaming).
- Expose reasoning effort as an explicit user-facing selector in Logic settings (not just hidden defaults).
- Prefer local model execution without localhost API dependency when possible.

## Patterns That Work
- Inspect SwiftUI file structure first, then make minimal style-only diffs.
- Use a reusable glass card modifier and button style so visual upgrades stay centralized and low-risk.
- Load custom menu bar/listening icons from `Bundle.module` PNG resources and set `isTemplate = true` so AppKit tinting still works.
- For macOS-native settings, use a dedicated `Settings` scene and trigger it from custom UI with `showSettingsWindow:` instead of building a faux settings popover.
- For feature knobs like model selection, keep one typed enum shared across picker, persisted settings, and API request serialization.
- For split-view settings UIs, keep selection state persisted in `UserDefaults` so users return to the last section/mode/model.
- For Tahoe-native visual passes in this app, move glass adoption into `PlatformAppearance.swift` and wrap the top-level scene content in one `GlassEffectContainer` before touching individual cards.
- For Tahoe-native layout cleanup here, prefer one glass surface per pane or section and let dense control groups live directly inside it instead of wrapping every subpanel in its own card.
- For audio capture in this app, keep the `AVAudioEngine` tap’s file write and frame conversion off the main actor behind a small locked helper; routing each buffer back through `@MainActor` is a hang risk.
- For translucent macOS window tuning in this app, ask for at least one screenshot with the desktop visible before adding synthetic background layers; black-background captures can misrepresent the actual glass effect.
- For fork-only workflows, remove `upstream` and verify `git remote -v` plus `git branch -r` to ensure no stale upstream references remain.
- For remote sync after history damage, push a synthetic linear commit (`commit-tree` with `origin/main` parent) instead of force-pushing broken ancestry.
- After broad scripted edits, use `git diff --name-only` and `git restore --worktree <file>` to quickly drop collateral edits while preserving unrelated staged work.
- For architecture refactors in this app, extract protocols around recorder/catalog/store/context services first; that makes the coordinator and post-transcription pipeline testable without going through the SwiftUI view model.
- For large native rewrites in this dirty repo, build the replacement in a separate shared source tree first so SwiftPM/Xcode can converge without trampling user edits under `Sources/VerbatimSwiftMVP/`.
- For third-party skill installs on this machine, validate the source `SKILL.md` first and use `install-skill-from-github.py --method git` when Python SSL certificate validation breaks the download path.
- For local GPT OSS cleanup here, the settings UI can exist before the behavior is real; carry logic settings through `ContextPack`, map local reasoning to Ollama `--think`, and hide visible thinking by default so dictated text is not polluted with model commentary.
- For local signed-app handoff in this repo, copy the Release bundle to `dist/`, run `xattr -cr` there, then ad-hoc sign with `codesign --deep --sign -`; signing directly from `build/Release` is more likely to inherit Xcode/synced-folder metadata that breaks `codesign`.

## Patterns That Don't Work
- Skipping persistent notes setup causes avoidable process drift.

## Domain Notes
- Repository is a SwiftUI app (`Package.swift`, `Sources/`).
- Current task: add Apple-style Liquid Glass visual enhancements while preserving current functionality.
- Model selection now uses `TranscriptionModel` and is persisted in user defaults.
- Source tree currently contains duplicate backup Swift files with numeric suffixes (excluded in `Package.swift`); avoid editing those and keep architectural changes in canonical files.
| 2026-03-13 | self | A capability-based coordinator refactor initially captured `self` inside `AppModel.init` before stored properties were fully initialized | When adding runtime resolver callbacks from `AppModel`, pass effective values at call time or set callbacks after initialization instead of capturing `self` during init |
| 2026-03-13 | self | A SwiftUI helper returning `some View` in `AppRootView` compiled locally only until a full build exposed that the `VStack` was never returned | For helper methods that build views, explicitly `return` the root view instead of relying on trailing-expression assumptions inside function bodies |
| 2026-03-13 | self | The earlier build exposed that the repo no longer contained any Style tab route or style settings despite prior assumptions | Before diagnosing a missing UI element, inspect the actual enum cases and content switch in the current worktree instead of trusting prior implementation claims |
| 2026-03-14 | self | The current mainline built target had a drastically simpler hotkey path than legacy, so restoring Fn/Globe required replacing the Carbon-only shortcut manager rather than just re-enabling hidden UI | When a user reports a missing feature after an architecture shift, compare the actual built target against the legacy feature source and port the behavior into the active shell instead of assuming the code is merely hidden |
| 2026-03-14 | self | I had previously treated the per-provider language and shared-core extraction as if they were already present, but the live worktree still had a single global language setting and no Rust bridge at all | Before layering another architecture step onto this repo, inspect the current built target and verify the bridge/core actually exist instead of building on prior implementation claims |
| 2026-03-14 | self | A large multi-hunk `apply_patch` against `AppModel.swift` failed even though the intended lines were present, because this worktree has enough drift that broad context matches are brittle | In this repo, patch large hot files like `AppModel.swift` in small targeted hunks after one exact slice read instead of retrying a monolithic patch |
| 2026-03-14 | self | During the semantic-trigger refactor, `TriggerMode` was still encoding legacy storage strings (`hold_to_talk`, `tap_to_toggle`), which would have broken the Rust bridge even though the Swift-side types looked correct | When a shared enum serves both persisted settings and FFI payloads, verify the encoded wire values explicitly instead of assuming the migration decoder also fixes outbound serialization |
